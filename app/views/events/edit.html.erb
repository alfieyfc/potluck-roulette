<div class="container mt-4">
  <h1><%= t('event.edit.title') %></h1>

  <%= render "form", event: @event %>

  <div class="row my-2">
    <div class="col offset-md-4 col-md-4">
      <%= button_to t('common.back_btn'), event_url(@event), method: :get, class: "btn-sm w-100" %>
    </div>
  </div>
</div>
